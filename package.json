{
  "name": "nodes-mysql-router",
  "version": "1.0.0",
  "description": "Route synchronization service between Linux kernel and MySQL",
  "main": "router.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.4.1",
    "express": "^5.2.1",
    "mysql2": "^3.20.0"
  }
}
